// @ts-check

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  advancedSidebar: [
    {
      type: 'autogenerated',
      dirName: '.', // This will generate sidebar from the current plugin's folder structure (docs-personalized/advanced)
    },
  ],
};

module.exports = sidebars;